  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1655842556550-6809c404ce9c?q=80&w=1170&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D);">
    <div class="container">
      <h1><%= @chat.title %>!</h1>
      <p></p>
    </div>
  </div>
<div class="container">
  <%= link_to "← Retour aux chats", chats_path, class: "back-link" %>

  <div id="messages_container">
    <% @chat.messages.each do |message| %>
      <p>
        <strong><%= message.role.capitalize %>:</strong>
        <%= raw(render_markdown(message.content)) %>
      </p>
    <% end %>
  </div>

  <div id="new_message_container">
    <%= simple_form_for [@chat, @message] do |f| %>
      <%= f.input :content, label: "Ask a question", required: true, placeholder: "How should I get started?" %>
      <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
