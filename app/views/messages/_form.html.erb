<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, label: "Ask a question", required: true, placeholder: "How should I get started?" %>
  <%= f.button :submit, 'Envoyer', class: 'btn btn-primary' %>
<% end %>
